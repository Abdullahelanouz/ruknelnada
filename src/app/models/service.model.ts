export interface Service {
  id: string;
  icon: string;
  imageUrl: string;
  translationKey: string;
}

export interface Testimonial {
  id: number;
  name: string;
  position: string;
  image: string;
  comment: string;
}